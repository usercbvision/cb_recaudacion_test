// src/reportWebVitals.js - versión simplificada sin dependencia de web-vitals
const reportWebVitals = (onPerfEntry) => {
  // Función vacía que no hace nada
  // Esto evita el error mientras mantenemos la compatibilidad con el código existente
  console.log('Web vitals reporting disabled');
};

export default reportWebVitals;